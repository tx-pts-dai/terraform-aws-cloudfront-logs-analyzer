name: Release on tag

on:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: write

jobs:
  release:
    uses: DND-IT/github-workflows/.github/workflows/gh-release.yaml@v2.7.0
    with:
      tag: ${{ github.ref_name }}
